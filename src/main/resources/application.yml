server:
  port: 8080

#database datasource configuration
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/godigital
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update